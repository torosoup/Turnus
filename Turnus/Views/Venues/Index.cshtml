@model IEnumerable<Turnus.Models.Venue>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.VenueStaffingRequirements)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ShiftDefinitions)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ScheduledDays)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(model => item.Name)
            </td>
            <td>
                @Html.DisplayFor(model => item.VenueStaffingRequirements)
            </td>
            <td>
                @Html.DisplayFor(model => item.ShiftDefinitions)
            </td>
            <td>
                @Html.DisplayFor(model => item.ScheduledDays)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
