@model Turnus.Models.Role

<div class="modal-header">
    <h5 class="modal-title">Edit Role</h5>
    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
</div>

<div class="modal-body">
    <form asp-action="Edit" asp-controller="Roles" method="post" id="role-edit-form">
        <input type="hidden" asp-for="Id" />
        <div class="mb-3">
            <label asp-for="Name" class="form-label"></label>
            <input asp-for="Name" class="form-control" />
            <span asp-validation-for="Name" class="text-danger"></span>
        </div>
        @* Venue and Department are predetermined by dashboard context. Only include hidden fields so values post with the form. *@
        <input type="hidden" asp-for="VenueId" />
        <input type="hidden" asp-for="DepartmentId" />
        <button type="submit" class="btn btn-primary">Save</button>
    </form>
</div>
