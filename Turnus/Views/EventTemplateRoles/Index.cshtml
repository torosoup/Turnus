@model IEnumerable<Turnus.Models.EventTemplateRole>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EventTemplateId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EventTemplate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RoleId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Role)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RequiredCount)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(model => item.EventTemplateId)
            </td>
            <td>
                @Html.DisplayFor(model => item.EventTemplate)
            </td>
            <td>
                @Html.DisplayFor(model => item.RoleId)
            </td>
            <td>
                @Html.DisplayFor(model => item.Role)
            </td>
            <td>
                @Html.DisplayFor(model => item.RequiredCount)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
